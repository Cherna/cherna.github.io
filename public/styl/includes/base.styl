* {
  margin: 0;
  padding: 0;
  border: 0;
}

html, body {
  @include size(100% 100%);
  font-family: 'Open Sans', Helvetica, sans-serif;
}

body.home {
  overflow: hidden;
}

a {
  color: inherit;
  text-decoration: none;
}